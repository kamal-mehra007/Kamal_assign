1) Configure the name and email 

        git config --global user.name "Kamal"
	git config --global user.email kamal.singh@weboniselab.com

2) To create a new local repo
	git init

3) List the files you've changed and those you still need to add or commit:
	git status

4) Delete the feature branch
	git branch -d <branchname>

5) Fetch and merge changes on the remote server to your working directory:
	git pull
